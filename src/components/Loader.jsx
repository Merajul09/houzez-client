import { HashLoader } from "react-spinners";

const Loader = () => {
    return (
        <div className="flex justify-center my-4">
            <HashLoader className="text-teal-400" />
        </div>
    );
};

export default Loader;